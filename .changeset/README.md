# Changesets

Create a changeset for any user-visible package change:

```bash
pnpm changeset
```

Describe the change and select the appropriate semantic-version bump. CI and
the release workflow use these files to prepare package versions and release
notes.
